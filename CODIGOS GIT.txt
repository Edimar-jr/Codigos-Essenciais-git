gitGprimeiro passo : git init
segundo passo : git add .
terceiro passo :git status
quarto passo :  git commit -m "mensagem do commit"
quinto passo : ciriar um repositorio na nuvem ( github)
sexto passo : git remote add origin ( colar o link da pagina repositorio )
setimo passo : git push
oitavo passo : copiar o cogigo que o git lhe deu 
nono passo : colar o codigo do git abaixo

conceito braunch.

git branch
git checkout nome-da-branch
git checkout -b "nome da branch de origem" "nome da nova branch"
git merge " branch a receber merge"
git pull


Atualizaçoes:
passo 1 : git status
passo 2: git add .
passo 3: git commit -m "mensagem da atualizaçao"
passo 4:  git push
gti


Saber versao atual:

 git reflog

versao anterios ( caso cometa erros )
 git reste ( copiar a versao a ser apagada )

ex : git reflog
  
 bfb5f66 (HEAD -> master, origin/master) HEAD@{0}: commit (initial): tags e comoponentes

copiar o codigo acima : bfb5f66

VOLTAR PARA VERSAO ATUAL 

git reset --hard ( codigo da versao atual )

 
                                                                                                  tututial top : https://www.youtube.com/watch?v=kB5e-gTAl_s